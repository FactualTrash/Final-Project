int logicalSize = 0;
 int maxDay = 23;
        int minDay = 20;
        int rangeDay = max - min + 1;
	for (int i = 0; i < 10; i++) {
            int totalDay = (int)(Math.random() * range) + min;
}

public void huntBearHunter(){
 int max = 5;
        int min = 1;
        int range = max - min + 1;
  
        // generate random numbers within 1 to 5
        for (int i = 0; i < 5; i++) {
            int rand = (int)((Math.random() * range + min);

if(rand == 1){

lblOutput.setText("the bear has successfully been killed! +5 meat");
//Add a day count.
}

else{

lblOutput.setText("the bear has mauled you and you’re bleeding internally and externally. You have died.");
}

        }
}
public void huntBearBoyscout(){
 
 if(job == 1){
 int max = 10;
        int min = 1;
        int range = max - min + 1;
  
        // generate random numbers within 1 to 10
        for (int i = 0; i < 10; i++) {
            int rand = (int)(Math.random() * range) + min;

if(rand = 1) {

lblOutput.setText("the bear has successfully been killed! +5 meat");
//Add a day count.
}
else {

lblOutput.setText("the bear has mauled you and you’re bleeding internally and externally. You have died.");
}
}
